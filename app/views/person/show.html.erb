<% content_for :page_title do %>
    <%= render "shared/page_title", title: 'People' %>
<% end %>

<% content_for :breadcrumb do %>
    <% link = link_to 'All People', person_index_path %>
    <%= render "shared/breadcrumb", items: [link, @person] %>
<% end %>

<div class="row">
    <div class="col-sm-4">
        <div class="nav flex-column nav-pills" id="v-pills-tab" role="tablist">
            <a class="nav-link active" id="v-pills-profile-tab" data-toggle="pill" href="#profile" role="tab">Profile</a>
            <a class="nav-link" id="v-pills-notifications-tab" data-toggle="pill" href="#notifications" role="tab">Notifications</a>
            <a class="nav-link" id="v-pills-dues-tab" data-toggle="pill" href="#dues" role="tab">Dues</a>
            <a class="nav-link" id="v-pills-public-tab" data-toggle="pill" href="#public" role="tab">Public Card</a>
            <a class="nav-link" id="v-pills-settings-tab" data-toggle="pill" href="#settings" role="tab">Settings</a>
        </div>
    </div>
    <div class="col-sm-8">
        <div class="tab-content">
            <div class="tab-pane active" id="profile" role="tabpanel">Profile</div>
            <div class="tab-pane" id="notifications" role="tabpanel">Notifications</div>
            <div class="tab-pane" id="dues" role="tabpanel">Dues</div>
            <div class="tab-pane" id="public" role="tabpanel">Public Card</div>
            <div class="tab-pane" id="settings" role="tabpanel">Settings</div>
        </div>
    </div>
</div>
